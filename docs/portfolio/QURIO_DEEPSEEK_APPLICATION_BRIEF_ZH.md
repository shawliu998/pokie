# Qurio × DeepSeek AGI 管培生投递说明

> 下面的产品和工程事实都可由仓库验证。个人贡献部分依据申请人的确认撰写：
> Qurio 的框架设计、任务规划、实现推进、检查验证和最终验收均由本人负责。
> 开源依赖、既有通用基础设施和 AI 编程工具仍按实际使用情况说明，不表述为本人
> 从零编写。

## 一页项目摘要

### 一句话

Qurio 是一个 AI 原生量化研究工作台：一个受约束、可验证的 Research Agent
能够制定研究计划、调用工具完成实验、根据证据修正，并在密封留出集失败时给出
“继续研究”，而不是包装成成功策略。

### 我想解决的问题

通用 Agent 很容易生成看似合理但无法复核的结论。量化研究又特别容易出现指标
口径不一致、过度拟合和测试集泄漏。因此我把 Agent 的自主性限制在研究决策层：

- 模型决定下一步研究动作；
- 注册工具限制 Agent 能做什么；
- 确定性内核统一计算全部指标；
- 训练阶段结束后只打开一次密封留出集；
- 每次实验、失败、修复和选择理由都进入结构化 Research Memory。

### 最能说明问题的案例

一次真实运行使用了 548 根 Kraken `BTCUSD · 4h` 收盘 K 线和 DeepSeek
`deepseek-v4-flash`，Mock 回退关闭。

Agent 先比较 A、B 两个候选，然后提出 C。C 的第一次工具调用违反了策略家族与
调整动作的合同，被系统拒绝；下一轮模型只修改了错误字段并成功完成实验。

训练指标排序为 C、B、A，但 C 的训练交易数为零。确定性的最小交易证据规则没有
盲从排名，而是选择 B。B 随后在新鲜密封留出集上失败，所以最终结论是
`revise_research`。

这个案例没有证明 alpha，却证明了系统能够：

- 让 Agent 的错误可见并可修复；
- 把模型判断与指标计算分开；
- 不把零交易候选包装成优秀策略；
- 不因留出集失败而反复打开测试数据。

### 作品里的三个画面

这三个画面不是同一次运行，也不应该连在一起解释。

- 首次打开时出现 `Validation evidence withheld`，表示当前保留运行无法核对最终候选
  身份，所以 Qurio 不展示 sealed holdout。这是系统对证据边界的正常处理，不是
  推荐的演示入口。
- `Open guided demo` 打开的是另一个只读的 Binance / DeepSeek 通过案例，用来让
  评审者点击 Research、Compare、Analyze 和 History。它不会再次调用模型，也不会
  改写原有证据。
- 投递材料仍以 Kraken / DeepSeek 的失败案例为主。这个案例最能说明无效调用被
  拒绝、修复范围可检查、零交易候选没有被误选，以及 holdout 失败后系统知道停止。

展示时先讲 Kraken 失败案例，再用 Guided Demo 补充界面操作。不要把两个案例说成
同一条运行记录。

### 收益数字怎么讲

Binance Guided Demo 中的 `+173.3%` 是一次 holdout 的年化输出，但它只包含 4 笔
交易，walk-forward 也只有 1 / 3 个窗口取得正收益。这个数字不能单独引用，更不能
解释为已经证明 alpha、盈利能力或统计显著性。

如果面试中需要提到它，我会把三项信息放在一起说：这个候选以 4 笔交易得到
`+173.3%` 年化 holdout 输出，walk-forward 只有 1 / 3 个正收益窗口；它通过的是
预先定义的验证门槛，不代表策略已经值得投入使用。

### 工程边界

- 桌面端：Tauri + React。
- 本地服务：FastAPI + Research Agent worker。
- 决策模型：DeepSeek 或受控的 OpenAI-compatible Provider。
- 数据：不可变数据版本，支持 CSV、Binance、Kraken 和受约束的 Nasdaq 路径。
- 验证：Python 合同/集成/安全/评测测试、Vitest、Playwright、Cargo 和 macOS
  原生门禁。
- 明确不做：Broker、实盘交易、任意 Python、Agent 市场和多 Agent 编排。

## 30 秒版本

我做了一个叫 Qurio 的 AI 原生量化研究工作台。它不是聊天套壳，而是让一个
Research Agent 在批准的计划和工具预算内做候选实验，确定性内核负责全部指标。
我最想展示的不是一次赚钱回测，而是一次真实 DeepSeek 运行：Agent 的工具调用
先被拒绝、随后完成窄修复，最后候选仍然在密封留出集失败，系统诚实地给出
“继续研究”。我希望用这个作品说明，我关心的不只是模型能生成什么，也关心怎样
把自主决策变成可验证、可复盘的产品。

## 90 秒版本

Qurio 的起点是一个很具体的问题：如果让大模型参与量化研究，怎样避免它只生成
漂亮的策略描述和回测数字？

我的处理方式是把模型和确定性系统分工。用户先选择保留的数据并批准研究计划；
一个 Research Agent 只能调用七个注册工具来生成候选、运行实验、比较和调整；
所有收益、回撤、交易和留出集结果都由同一个确定性内核计算。

在主案例中，系统使用真实 Kraken BTCUSD 四小时数据和 DeepSeek。Agent 做了
A/B 两个候选后，根据训练证据提出 C。C 的第一次工具调用违反合同，被服务端
拒绝；模型下一轮只修正了错误的 replan action，没有改变其他参数。训练排序是
C、B、A，但 C 没有产生交易，所以确定性证据规则选择 B，而不是机械选择第一名。

最后系统只打开一次密封留出集，B 失败。Qurio 没有把这次运行标记成系统失败，
也没有继续对测试集调参，而是保留完整证据并建议 revise research。

这件事对我最重要的地方是：Agent 的价值不只是“能行动”，而是行动边界、错误、
修复和停止理由都能被人理解和验证。

## 五分钟展开顺序

1. 先讲问题：LLM 结论不可复核与量化过拟合。
2. 画出边界：数据 → 批准计划 → Agent → 注册工具 → 确定性内核 → 密封留出集。
3. 展示 Decision Ledger 中失败的 Candidate C 工具调用。
4. 展示修复后的 C 和第二次训练比较。
5. 解释为什么训练排名第一的 C 没被选择。
6. 展示 holdout fail 与 `revise_research`。
7. 最后讲取舍：为什么单 Agent、为什么不允许任意 Python、下一步怎样做 Agent
   评测而不是继续堆功能。

## 高频追问

### 为什么不用多 Agent？

当前核心问题是让一条证据 → 调整 → 决策链可理解。一个 Agent 加专业工具更容易
归因，也便于控制预算。只有当独立 specialist loop 能在相同预算下稳定提高结果，
我才会引入多 Agent。

### 如何避免 holdout 泄漏？

候选生成、调整和比较只使用训练分区。最终候选冻结后才执行一次密封留出集评估；
holdout 结果不能回流到同一研究版本继续调参。继续研究会创建有清晰 lineage 的
新版本。

### 为什么相信回测指标？

不要求相信模型。模型只选择研究动作，指标由唯一确定性计算路径产生，使用固定的
执行时点、成本和候选 canonical key；UI、比较和证据导出都读取同一份结果。

### Agent 调用工具失败怎么办？

工具层返回有类型的错误和被拒参数。下一轮模型获得这份反馈；系统同时限制修复
预算、拒绝重复无效动作，并记录 correction delta。主案例中只发生了一次失败，
下一轮只改动了错误的 replan action。

### 这个项目目前最大的不足是什么？

目前证明的是单次完整工程边界，不是通用 Agent 能力或用户需求。下一步最有价值的
工作是冻结一组研究任务，对 DeepSeek 与确定性基线比较任务完成率、工具调用有效率、
修复率、重复候选率、成本和失败类型，而不是再增加新页面。

## 个人贡献

### 一句话版本

Qurio 从问题定义到最终交付都由我负责：我设计研究框架和产品边界，拆解任务，
推进实现，检查结果，并决定哪些结论可以写进作品。

### 30 秒口述版

这个项目是我从问题定义开始一路推进的。我先定下最重要的规则：模型可以决定下一步
研究动作，但不能自己生成权威指标；所有结果都必须经过同一套确定性计算和留出集
检查。之后从架构、开发计划到具体实现、调试、测试和作品整理，都是我负责。开发中
我使用了 AI 编程工具提高速度，但需求怎么拆、方案怎么取舍、代码是否接受、结果能
不能对外讲，最后都由我判断。

### 简历版

**Qurio｜独立完成的 AI 原生量化研究工作台**

- 独立负责产品问题定义、研究框架、系统架构、开发计划、实现推进和最终验收。
- 设计受批准计划和注册工具约束的单 Research Agent，并将模型决策与确定性指标
  计算分开，保留实验、失败、修复和停止理由。
- 完成桌面工作台、本地服务、Agent worker、数据与验证路径的集成，用真实
  DeepSeek 运行和自动化测试检查关键主张。

### 面试展开版

我在 Qurio 里不是只提需求，也不是只负责某个模块。这个项目的几个关键角色都是我
自己承担的。

第一步是把问题定清楚。我没有把目标写成“让大模型生成策略”，而是先解决模型结论
无法复核、量化指标容易漂移、留出集容易被反复使用这些问题。基于这个判断，我确定
了单 Agent、注册工具、统一指标计算和一次性 sealed holdout 的边界。

第二步是把方向拆成能实现和验收的任务。我安排 Data、Research、Compare、Analyze
和 History 的顺序，先完成一条真实数据能够走通的主路径，再处理界面和申请材料。

第三步是推进实现。我负责各部分怎么连接、哪些合同必须共用、哪些功能不能加入，
也处理运行中暴露出来的问题。主案例里 Agent 的错误调用、窄修复、候选选择和
holdout 失败，都是我保留下来的真实结果，没有为了展示效果改写成成功案例。

最后是检查和验收。我会对照产品边界看代码变更，运行相关测试，核对证据文件和
哈希，并检查公开材料是否超出了实际结果。对我来说，完成项目不只是功能能运行，
还包括知道哪些结论有证据、哪些不能讲。

### AI 辅助边界说明

开发过程中我使用了 Codex、ChatGPT 等 AI 编程工具，主要用于局部实现、测试草稿、
资料整理和代码复审。我不会直接把模型输出当成最终结果：任务范围、架构取舍、代码
整合、测试标准、问题修复和最终验收都由我负责。

项目依赖 Tauri、React、FastAPI 等开源技术，也复用了既有的通用工程基础。我的贡献
不是重新编写这些依赖，而是确定 Qurio 要解决的问题，设计量化研究框架，把各部分
组织成一个可运行、可验证的产品，并负责交付质量。

## 简历项目描述

只有在与实际贡献相符时使用：

**Qurio｜AI 原生量化研究工作台**

- 设计受批准计划、注册工具和实验预算约束的单 Research Agent，实现
  A/B → 观察 → C 调整 → 决策的可复盘研究链路。
- 将 LLM 研究决策与确定性量化指标计算分离，使用不可变数据身份、训练内
  walk-forward 证据和一次性密封 holdout 降低指标漂移与测试集泄漏。
- 完成 React/Tauri、FastAPI、Agent worker 与本地持久化集成；保留真实
  DeepSeek 工具调用修复和 holdout 失败案例，并用多层自动化测试验证。

## 投递链接顺序

1. GitHub 仓库首页。
2. 90 秒演示视频。
3. Kraken/DeepSeek 主案例。
4. 架构图。
5. 脱敏 JSON 证据。

不要先发安装包，也不要先讲阶段编号、审计字段或代码行数。先让对方理解问题和
关键判断，再提供工程深度。
